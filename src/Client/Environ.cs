namespace Experian.Api.Client
{
    /// <summary>
    /// The server environments, note Environ used to reduce name collision.
    /// </summary>
    public enum Environ
    {
        Sandbox,
        Staging,
        Production,
    }
}
